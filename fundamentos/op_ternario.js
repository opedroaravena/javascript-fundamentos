const resultado = nota => nota >= 7 ? 'Aprovadao' : 'Reprovadao'

console.log(resultado(3))

// utilizando operador ternário simples

const ex = ponto => {
    return ponto >= 8 ? 'Aprovadao' : 'Reprovadao'
}

console.log(ex(3))

// utilizando arrow function

console.log(5 <= 5)